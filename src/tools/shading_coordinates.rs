/// Un fichier qui contiens les méthodes permettant de passer d'un système de coordonnées
/// cartésien à un système de coordonnées dont les vecteurs de bases sont :
/// `n` - le vecteur normal à la surface (normale géométrique), de coordonnées (0,0,1)
/// `t` -
///
/// Ce système de coordonnées est le système de coordonnées de réflexion locale.

pub struct Non {}
